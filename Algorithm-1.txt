create array “marks” with 30 elements
set maxMark to 0
set minMark to 30
set counter to 0
int mark
String asstName

ask the user for the assignment name
store assignment name in asstName

repeat the following as long as the counter is <30:
	ask user for mark or if they are finished
	if user inputs "done":
		break out of the loop
	try: convert the user input mark into an integer
	catch exception: print "Please enter a number"
	
	if mark < 0 or mark > 30:
		print "Please enter a number between 0 and 30"

	add mark into the “marks” array at position counter
	increment counter by 1

	if mark > maxmark then
		set maxMark to mark
	if mark < minMark then
		set minMark to mark

print assignment name followed by students marks
print maxMark " is the Maximum mark"
print minMark " is the Minimum mark"

